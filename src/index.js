import React from 'react';
import ReactDOM from 'react-dom/client';
import ManufacturingTimerApp from './components/ManufacturingTimerApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ManufacturingTimerApp />
  </React.StrictMode>
);